# API Guide

*Placeholder — content in development.*

Will port the existing API guide (`documentation/API_guide.md`, served at
`/static/api_guide.html`) into this site, and add the endpoint families not yet documented
there: `/api/area`, `/api/similarity`, `/api/basin-preview`, `/api/whg-reconcile`,
`/api/explorer/*`, `/api/whc-*`. Interactive schema (Swagger): `/api/schema`.

See `docs/design/DOCSv4 — TODO.md` §5.7.
