# Session handoff — 2026-08-31

Continuing work on **EDOPS** (`/Users/karlg/Documents/repos/_edops`). Read `CLAUDE.md` first,
then `docs/edop/workbench/workplan_africa.md` (the active work).

## Two live threads, both on branch `wb_africa` (cut from `v04`, off `main`)

### 1. v0.4 deploy — deferred, ~mid-Sept. Don't touch unless Karl raises it.
Full blue-green dry run validated on `edops04.computingplace.org` (service `edops-v4` :8004, venv
`cedop-v4`, additive DB objects already in the shared live `cedop`). Live v0.3 on :8001 untouched.
Cutover = merge `wb_africa` → `v04`, redeploy `edops-v4`, re-verify, flip one nginx `proxy_pass`
line. Per-step record + **resume at step 13**: `logs/session_log_20260830_deploy.md`. Checklist:
`MAINTAIN_DEPLOY.md` (repo root, gitignored). The LMR-baseline fix is in `v04` but not yet pulled
to the staging server — goes up with the pre-cutover batch.

### 2. African Regions Workbench tab — active. For Karl's Braga talk (2026-09-23).
Built from `docs/design/_workbench/prospectus_AfricanRegions.md` (draft — nothing in stone).
Work order log: `docs/edop/workbench/workplan_africa.md`.

- **WO01 done** — new tab; layout is map-left / info-right within the 6/6 row; `#afr-map` is a
  live MapLibre map (own instance, lazy-init) with the shared Protomaps light basemap
  (`app/static/js/pm_basemap.js`, `pmLightStyle()`), fit to Africa. Full Leaflet→MapLibre port of
  the rest of `workbench.html` is deferred; this tab keeps its own map.
- **WO02 done** — 34 Lovejoy pre-colonial African subregions on the map (fill + outline). Click a
  region → `renderAfrRegion()` fills `#afr-right` with name / macro / LPF blurb / article
  rationale. `#afr-right` is the single click-output target — D-PLACE society-marker clicks will
  render there too. Artifacts: `app/static/workbench/lovejoy_regions.geojson` +
  `lovejoy_region_notes.json`, built by `scripts/edop/workbench/build_lovejoy_{geojson,notes}.py`.
  **WO02 follow-ups** (in the workplan): 7 `needs_review` rationale entries to hand-finish against
  `data/lovejoy/lovejoy_regions_prose.txt`; blurb vs rationale are often the same opening
  (diff-and-show-one); minor PDF-wrap cruft in a few rationale strings. `whg_export_bug_issue.md`
  is a draft for Karl to file against WHG.
- **WO03 — not designed yet.** Scope it with Karl. Candidates from the prospectus §4:
  variable-painting `<select>`s + choropleth over L6/L8 basins (reuse `sandbox.html`'s `_atlasMap`
  pattern — pmtiles basins + feature-state paint from `/api/explorer/values`); the D-PLACE society
  marker layer (`cedop.dplace.*`, local + prod; the Societies tab's `/api/societies` is the model)
  + the regions-vs-societies click-mode toggle.

## How the work goes

- WO drafts get Karl's approval before execution; draft further WOs into `workplan_africa.md`.
- **Karl reviews every UI change in the browser before you commit** — his local server is always
  running; stop and hand off the review, don't commit unseen. (Missed this once this session.)
- Layout/CSS iteration is skunkworks: skip the full suite, commit after each visual change once
  Karl OKs. `pytest tests/` at real gates (Python changes, merge points). No failing tests, ever.
- No Opus — draft WOs yourself.
- Server work (deploy only): read-only inventory via `ssh kgeographer-1` is yours; `sudo` / DB /
  rsync steps go to Karl as copy-paste command blocks (`ssh 'sudo …'` has no tty).
- Notebooks: Karl runs them cell-by-cell; never run them via Bash.
- `whg_staging` is a separate local Postgres DB (same host/creds as `cedop`, `dbname=whg_staging`);
  build scripts read it locally and emit static artifacts — nothing at app runtime touches it.

## Start here

Confirm state (`git log --oneline -6` on `wb_africa`, load the tab locally), then work with Karl
to scope WO03. The WO02 follow-ups are small and can slot in whenever.
